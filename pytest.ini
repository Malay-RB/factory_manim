[pytest]
pythonpath = src
markers =
    unit: marks tests as unit tests (fast, isolated)
    integration: marks tests as integration tests (config-driven, multiple components)
    e2e: marks tests as end-to-end tests (full pipeline)